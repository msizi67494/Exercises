import React, { Fragment } from 'react';
import {AppBar, Toolbar, Typography} from '@material-ui/core';

export default props => 
   
        
            <Fragment>
                <AppBar style={{marginBottom: 30}}>
                    <Toolbar>
                        <Typography color="inherit" variant="headline">
                            Smart Register
                        </Typography>
                    </Toolbar>
                </AppBar>
            </Fragment>
    
    

